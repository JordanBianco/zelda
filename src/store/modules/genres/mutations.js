export const GET_GENRES = (state, genres) => {
    state.genres = genres
}

export const GET_GENRE = (state, genre) => {
    state.genre = genre
}

export const GET_GENRE_BOOKS = (state, books) => {
    state.books = books
}
