export const books = (state) => {
    return state.books
}

export const book = (state) => {
    return state.book
}

export const similarBooks = (state) => {
    return state.similarBooks
}

export const reviews = (state) => {
    return state.reviews
}

export const selectedReviews = (state) => {
    return state.selectedReviews
}

export const errors = (state) => {
    return state.errors
}