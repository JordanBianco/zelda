export const is_auth = (state) => {
    return state.is_auth
}

export const user = (state) => {
    return state.user
}

export const token = (state) => {
    return state.token
}

export const errors = (state) => {
    return state.errors
}